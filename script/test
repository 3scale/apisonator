#!/bin/bash

SCRIPT_DIR=$(dirname "$(readlink -f $0)")
. ${SCRIPT_DIR}/lib/functions

function cleanup()
{
    if [ -z "$CI" ]; then
        stop_services
    fi
}

trap 'cleanup' INT

export RACK_ENV=test

start_services

bundle exec rake
EXITVAL=$?

cleanup

exit ${EXITVAL}
