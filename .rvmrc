rvm use ruby-1.9.3-p327-falcon
rvm gemset use 3scale_backend

# install bundler
if ! which bundle 1>/dev/null ; then
  gem install bundle
fi

# install rake
if ! which rake 1>/dev/null ; then
  gem install rake
fi

bundle check 1>/dev/null

if [ "$?" -ne "0" ]; then
  bundle install
fi
